# Notification System Design

## Architecture
Frontend (React) → Backend (Node/Express) → Database

## Features
- Send notifications
- View notifications

## Flow
User → UI → API → Backend → Response → UI

## Logging
Custom logging middleware logs all API calls